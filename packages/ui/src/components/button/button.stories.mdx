import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import RfxButton from './button.vue'

<Meta
  title="Button"
  component={RfxButton}
  argTypes={{
    kind: { control: { type: 'radio', options: ['primary', 'secondary'] } },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RfxButton },
  template: '<rfx-button>Click me!</rfx-button>',
})

# Button

This is the Reflex button

<Canvas>
  <Story
    name="Rounded"
    args={{
      rounded: true,
      color: '#f00',
      label: 'A Button with rounded edges',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
