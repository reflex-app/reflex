import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { RfxIcon } from '~reflex/ui'

<Meta
  title="Icon"
  component={RfxIcon}
  argTypes={{
    icon: {
      control: { type: 'radio' },
      options: ['star', 'activity', 'alert-circle'],
      defaultValue: 'star',
    },
    size: {
      control: { type: 'radio' },
      options: ['xs', 'sm', 'md', 'lg'],
      defaultValue: 'sm',
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RfxIcon },
  template: '<rfx-icon v-bind="$props"></rfx-icon>',
})

# Icon

<Canvas>
  <Story name="Icon">{Template.bind({})}</Story>
</Canvas>

## Props

<ArgsTable story="Icon" />
