import { createCss } from '@stitches/core'

export const {
  styled,
  css,
  global,
  keyframes,
  getCssString,
  theme,
} = createCss({
  theme: {},
  media: {},
  utils: {},
  prefix: '',
  insertionMethod: 'append | prepend | () => {}',
  themeMap: {},
})
